services:
  libretranslate:
    image: libretranslate/libretranslate:latest
    restart: always
    ports:
      - "6000:5000" # external:internal
    environment:
      - LT_LOAD_ONLY=en,fr,hi,mr # only load English, French, Hindi, and Marathi (saves memory)
